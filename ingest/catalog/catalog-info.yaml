apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: ingest
  description: Upload interface for weather reports
  tags:
    - app-accelerator
    - beta
    - function
    - buildpacks
    - knative
    - tanzu
    - python
  annotations:
    'backstage.io/kubernetes-label-selector': 'app.kubernetes.io/part-of=ingest'
spec:
  type: service
  lifecycle: experimental
  owner: default-team